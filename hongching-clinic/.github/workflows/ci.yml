name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: hongching-clinic/package-lock.json
      - run: npm ci
        working-directory: hongching-clinic
      - run: npm test
        working-directory: hongching-clinic
      - run: npm run build
        working-directory: hongching-clinic
